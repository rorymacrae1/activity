-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.accommodation (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  type text CHECK (type = ANY (ARRAY['hotel'::text, 'apartment'::text, 'chalet'::text, 'hostel'::text])),
  name text,
  stars smallint CHECK (stars >= 1 AND stars <= 5),
  ski_in_out boolean DEFAULT false,
  kids_club boolean DEFAULT false,
  price_per_night_gbp numeric,
  min_nights smallint,
  catered boolean DEFAULT false,
  booking_url text,
  created_at timestamp with time zone DEFAULT now(),
  source text DEFAULT 'openstreetmap'::text,
  CONSTRAINT accommodation_pkey PRIMARY KEY (id),
  CONSTRAINT accommodation_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.airport_link (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  iata_code text NOT NULL,
  airport_name text,
  transfer_mins integer,
  road_winding_score smallint CHECK (road_winding_score >= 1 AND road_winding_score <= 10),
  direct_flights_available boolean DEFAULT false,
  top_airlines text,
  shuttle_available boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  uk_departure_airports text,
  airlines text,
  flight_time_mins smallint,
  seasonal_only boolean DEFAULT false,
  ski_charter_available boolean DEFAULT false,
  frequency text,
  CONSTRAINT airport_link_pkey PRIMARY KEY (id),
  CONSTRAINT airport_link_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.cost_data (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  lift_pass_daily_gbp numeric,
  lift_pass_weekly_gbp numeric,
  mountain_lunch_gbp numeric,
  beer_on_mountain_gbp numeric,
  ski_rental_daily_gbp numeric,
  ski_school_half_day_gbp numeric,
  overall_cost_index smallint CHECK (overall_cost_index >= 1 AND overall_cost_index <= 5),
  year smallint NOT NULL DEFAULT (EXTRACT(year FROM now()))::smallint,
  created_at timestamp with time zone DEFAULT now(),
  resort_name text,
  CONSTRAINT cost_data_pkey PRIMARY KEY (id),
  CONSTRAINT cost_data_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.facility (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  type text CHECK (type = ANY (ARRAY['restaurant'::text, 'bar'::text, 'supermarket'::text, 'pool'::text, 'spa'::text, 'hotel'::text, 'ski_school'::text, 'ski_rental'::text, 'other'::text])),
  name text,
  veggie_options boolean DEFAULT false,
  vegan_options boolean DEFAULT false,
  avg_price_gbp numeric,
  rating numeric,
  ski_to_door boolean DEFAULT false,
  source text,
  created_at timestamp with time zone DEFAULT now(),
  review_count integer,
  english_speaking boolean DEFAULT false,
  kids_lessons boolean DEFAULT false,
  private_available boolean DEFAULT true,
  instructor_notes text,
  whiteout_activity boolean DEFAULT false,
  apres_ski boolean DEFAULT false,
  nightlife_level smallint CHECK (nightlife_level >= 1 AND nightlife_level <= 5),
  CONSTRAINT facility_pkey PRIMARY KEY (id),
  CONSTRAINT facility_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.famous_run (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  name text NOT NULL,
  local_name text,
  difficulty text CHECK (difficulty = ANY (ARRAY['easy'::text, 'intermediate'::text, 'hard'::text, 'extreme'::text, 'off_piste'::text])),
  length_km numeric,
  vertical_drop_m integer,
  top_altitude_m integer,
  bottom_altitude_m integer,
  world_famous boolean DEFAULT false,
  race_piste boolean DEFAULT false,
  race_event text,
  night_skiing boolean DEFAULT false,
  why_famous text,
  best_for text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT famous_run_pkey PRIMARY KEY (id),
  CONSTRAINT famous_run_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.flight_route (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  destination_airport text NOT NULL,
  destination_iata text NOT NULL,
  uk_airport text NOT NULL,
  uk_iata text NOT NULL,
  airline text NOT NULL,
  flight_time_mins smallint,
  seasonal boolean DEFAULT true,
  ski_season_only boolean DEFAULT false,
  frequency text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT flight_route_pkey PRIMARY KEY (id),
  CONSTRAINT flight_route_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  email text,
  display_name text,
  avatar_url text,
  home_airport text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.resort (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  country text NOT NULL,
  region text,
  location USER-DEFINED,
  altitude_base_m integer,
  altitude_top_m integer,
  car_free_town boolean DEFAULT false,
  style text CHECK (style = ANY (ARRAY['traditional'::text, 'modern'::text, 'mixed'::text])),
  snow_sure_score smallint CHECK (snow_sure_score >= 1 AND snow_sure_score <= 5),
  total_km_piste integer,
  blue_runs integer,
  red_runs integer,
  black_runs integer,
  beginner_area boolean DEFAULT false,
  snow_park boolean DEFAULT false,
  off_piste boolean DEFAULT false,
  disability_access boolean DEFAULT false,
  olympic_history boolean DEFAULT false,
  olympic_detail text,
  embedding USER-DEFINED,
  last_updated timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  off_piste_score smallint CHECK (off_piste_score >= 1 AND off_piste_score <= 5),
  backcountry_access boolean DEFAULT false,
  guide_required boolean DEFAULT false,
  freeride_world_tour boolean DEFAULT false,
  off_piste_areas text,
  train_accessible boolean DEFAULT false,
  eurostar_direct boolean DEFAULT false,
  train_journey_hours numeric,
  train_route_summary text,
  drive_hours_from_london numeric,
  sustainability_score smallint CHECK (sustainability_score >= 1 AND sustainability_score <= 5),
  sustainability_notes text,
  wheelchair_accessible boolean DEFAULT false,
  adaptive_ski_school boolean DEFAULT false,
  accessibility_notes text,
  apres_ski_rating smallint CHECK (apres_ski_rating >= 1 AND apres_ski_rating <= 5),
  apres_ski_notes text,
  webcam_url text,
  snow_report_url text,
  heli_skiing_available boolean DEFAULT false,
  heli_skiing_legal text CHECK (heli_skiing_legal = ANY (ARRAY['yes'::text, 'no'::text, 'limited'::text, 'loophole'::text, 'nearby'::text])),
  heli_skiing_notes text,
  heli_skiing_operator text,
  heli_skiing_cost_gbp integer,
  night_skiing_available boolean DEFAULT false,
  night_skiing_pistes smallint,
  night_skiing_km numeric,
  night_skiing_days text,
  night_skiing_until text,
  night_skiing_cost_gbp smallint,
  night_skiing_notes text,
  snowmaking_cannon_count smallint,
  snowmaking_coverage_pct smallint CHECK (snowmaking_coverage_pct >= 0 AND snowmaking_coverage_pct <= 100),
  snowmaking_km_covered numeric,
  snowmaking_reliability smallint CHECK (snowmaking_reliability >= 1 AND snowmaking_reliability <= 5),
  glacier_skiing boolean DEFAULT false,
  min_altitude_m smallint,
  max_altitude_m smallint,
  snow_sure_rating smallint CHECK (snow_sure_rating >= 1 AND snow_sure_rating <= 5),
  snowmaking_notes text,
  hero_image text,
  lat numeric,
  lng numeric,
  continent text,
  CONSTRAINT resort_pkey PRIMARY KEY (id)
);
CREATE TABLE public.resort_category (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  resort_id uuid,
  category text NOT NULL,
  rank integer,
  reason text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT resort_category_pkey PRIMARY KEY (id),
  CONSTRAINT resort_category_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.resort_content (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  field_key text NOT NULL,
  plain_english_value text,
  raw_value text,
  generated_at timestamp with time zone DEFAULT now(),
  claude_model_version text,
  CONSTRAINT resort_content_pkey PRIMARY KEY (id),
  CONSTRAINT resort_content_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.season_timing (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL UNIQUE,
  season_open date,
  season_close date,
  best_weeks text,
  avoid_weeks text,
  good_christmas boolean DEFAULT false,
  good_late_season boolean DEFAULT false,
  good_long_weekend boolean DEFAULT false,
  school_hols_busy text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT season_timing_pkey PRIMARY KEY (id),
  CONSTRAINT season_timing_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.slope_data (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL UNIQUE,
  total_km integer,
  blue_km integer,
  red_km integer,
  black_km integer,
  lifts_total integer,
  gondolas integer,
  chairlifts integer,
  snow_park boolean DEFAULT false,
  half_pipe boolean DEFAULT false,
  mogul_field boolean DEFAULT false,
  off_piste_guided boolean DEFAULT false,
  snow_park_features integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT slope_data_pkey PRIMARY KEY (id),
  CONSTRAINT slope_data_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.spatial_ref_sys (
  srid integer NOT NULL CHECK (srid > 0 AND srid <= 998999),
  auth_name character varying,
  auth_srid integer,
  srtext character varying,
  proj4text character varying,
  CONSTRAINT spatial_ref_sys_pkey PRIMARY KEY (srid)
);
CREATE TABLE public.transport_option (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  type text CHECK (type = ANY (ARRAY['train'::text, 'bus'::text, 'car'::text, 'ferry'::text, 'shuttle'::text])),
  description text,
  nearest_station text,
  station_distance_km integer,
  eurostar_viable boolean DEFAULT false,
  parking_available boolean DEFAULT false,
  parking_spaces integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT transport_option_pkey PRIMARY KEY (id),
  CONSTRAINT transport_option_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);
CREATE TABLE public.user_favorites (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  resort_id text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_favorites_pkey PRIMARY KEY (id),
  CONSTRAINT user_favorites_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.user_preferences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  has_completed_onboarding boolean DEFAULT false,
  trip_type text CHECK (trip_type = ANY (ARRAY['solo'::text, 'couple'::text, 'family'::text, 'friends'::text])),
  group_abilities ARRAY DEFAULT '{}'::text[],
  budget_level text CHECK (budget_level = ANY (ARRAY['budget'::text, 'mid'::text, 'premium'::text, 'luxury'::text])),
  regions ARRAY DEFAULT '{}'::text[],
  crowd_preference integer DEFAULT 3 CHECK (crowd_preference >= 1 AND crowd_preference <= 5),
  family_vs_nightlife integer DEFAULT 3 CHECK (family_vs_nightlife >= 1 AND family_vs_nightlife <= 5),
  snow_importance integer DEFAULT 3 CHECK (snow_importance >= 1 AND snow_importance <= 5),
  language text DEFAULT 'en'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_preferences_pkey PRIMARY KEY (id),
  CONSTRAINT user_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.visited_resorts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  resort_id text NOT NULL,
  visited_at timestamp with time zone DEFAULT now(),
  notes text,
  rating smallint CHECK (rating >= 1 AND rating <= 5),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT visited_resorts_pkey PRIMARY KEY (id),
  CONSTRAINT visited_resorts_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.weather_month (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  resort_id uuid NOT NULL,
  month smallint NOT NULL CHECK (month >= 1 AND month <= 12),
  avg_temp_c numeric,
  avg_wind_kph numeric,
  wind_chill_c numeric,
  avg_bluebird_days smallint,
  avg_snowfall_cm numeric,
  snow_depth_cm numeric,
  visibility_score smallint CHECK (visibility_score >= 1 AND visibility_score <= 5),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT weather_month_pkey PRIMARY KEY (id),
  CONSTRAINT weather_month_resort_id_fkey FOREIGN KEY (resort_id) REFERENCES public.resort(id)
);